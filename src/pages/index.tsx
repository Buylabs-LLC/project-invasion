import Controller from "@/components/controller"
import Config from "@config"

export default function Home() {
  return (
    <main className="flex flex-col justify-center w-full gap-8 min-h-screen">
        <Controller />
    </main>
  )
}
