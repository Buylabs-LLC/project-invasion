-- Importing other files to be used with a global package and can be easily sperated if needed.
local types = require('typeValidation')
local Router = require('router')
local Master = require('master')
local Turtle = require('turtle')